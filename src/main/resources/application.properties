spring.application.name=web
# src/main/resources/application.properties

# 1. Database URL: Must use 'jdbc:mysql' prefix, default port is 3306.
# Add serverTimezone=UTC to avoid timezone warnings in modern MySQL versions.
spring.datasource.url=jdbc:mysql://localhost:3306/test?serverTimezone=UTC

# 2. Database Username (Replace 'jose' with your MySQL username)
spring.datasource.username=jose

# 3. Database Password (Replace '123456' with your MySQL password)
spring.datasource.password=123456

# 4. Driver Class Name (Optional, as Spring Boot usually auto-detects, but good practice)
# Use the modern driver class for MySQL Connector/J 8.0+
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ... (other properties)
JPA/Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true